// Fill these in once you've deployed the license service (step 2) and have
// a Windows installer URL to link to (step 5). Nothing else in this folder
// needs to change when you do.
window.TEASY_CONFIG = {
  LICENSE_SERVICE_URL: "https://your-license-service.up.railway.app",
  WINDOWS_DOWNLOAD_URL: "#", // point this at the .exe/installer once it's hosted somewhere (e.g. a GitHub release)
};
